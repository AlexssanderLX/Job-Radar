# Optional indexed web search. Leave blank to use direct connectors and manual fallbacks only.
WEB_SEARCH_PROVIDER=serper
WEB_SEARCH_API_KEY=
WEB_SEARCH_ENDPOINT=https://google.serper.dev/search

# Local database override (optional)
# DATABASE_URL=sqlite+aiosqlite:///./job_radar.db
